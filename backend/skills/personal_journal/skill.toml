[skill]
name = "personal_journal"
version = "1.0.0"
description = "Read, write, append, list, and move personal journal entries (YYYY-MM-dd.md) in data/memories/main/journal/personal"
enabled = true

[skill.permissions]
network = false
filesystem = true
# Writes confined to data/memories/main/journal/personal via journal_io path checks.
